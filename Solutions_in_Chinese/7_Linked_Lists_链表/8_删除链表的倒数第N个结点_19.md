# LeetCode 19. 删除链表的倒数第 N 个结点（Remove Nth Node From End of List）

---

# 一、题目描述
![alt text](<../../Snap_for_Questions/7_Linked_Lists_链表/截屏2026-06-28 17.02.25.png>)

直接答案：
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -> Optional[ListNode]:
        # 这是一道dummy + 双指针快慢指针的题，并且蕴含一个数学逻辑，快指针先移动n+1后 快慢指针同时运动到，快指针到末尾后，慢指针就在结点的前一个位置。
        dummy = ListNode(0)
        dummy.next = head
        fast = dummy
        slow = dummy

        # 迭代循环先移动快指针,这里表示 循环n+1次。
        for _ in range( n+1 ):
            fast = fast.next
        
        # 快慢双指针开始同时移动到快指针到None的情况
        while fast:
            fast = fast.next
            slow = slow.next
        
        # 快指针到None之后，则跳出循环，进行slow指针的删除操作
        slow.next = slow.next.next

        return dummy.next
```

给你一个单链表的头节点 `head` 和一个整数 `n`。

要求：
> 删除链表的**倒数第 n 个节点**，并返回删除后的链表头节点。

例如：
```
原链表：
1 → 2 → 3 → 4 → 5
```

如果：

```
n = 2
```

表示删除：

```
倒数第二个节点
4
```

结果：

```
1 → 2 → 3 → 5
```

---

# 二、什么叫倒数第 N 个节点？

例如：

```
1 → 2 → 3 → 4 → 5
```

正数：

```
第1个：1

第2个：2

第3个：3

第4个：4

第5个：5
```

倒数：

```
倒数第1个：5

倒数第2个：4

倒数第3个：3

倒数第4个：2

倒数第5个：1
```

因此：

```
n = 2
```

就是删除：

```
4
```

---

# 三、最容易想到的方法（两次遍历，可以快速略过了解）

第一遍：

```
统计链表长度 len
```

例如：

```
1 → 2 → 3 → 4 → 5
len = 5
```

删除：
```
倒数第2个
```

实际上就是：

```
正数：

第

5 - 2 + 1

=

4

个节点
```

然后：
第二遍：
走到：

```
第3个节点
```

修改：

```
next
```

即可。

时间：

```
O(n)
```

虽然也是：

```
O(n)
```

但是：需要：

```
遍历两次
```

面试更喜欢：

```
一次遍历
```

---

# 四、本题核心思想（★★★★★）

采用：

> **快慢指针 + Dummy（虚拟头节点）**

整体流程：

```
Dummy
↓
fast 先走 n+1 步
↓
slow 从 Dummy 开始
↓
fast、slow 同时走
↓
fast 到达末尾
↓
slow 正好位于
待删除节点前一个位置
↓
修改 next
```

这是链表删除倒数节点最经典模板。

---

# 五、为什么要使用 Dummy？

假设：

```
1 → 2 → 3
```

删除：

```
倒数第3个
也就是：
头节点
```

如果：
没有 Dummy：

```
head
被删除
```

需要：

```
特殊判断
```

非常麻烦。

有 Dummy：

```
dummy
↓
0 → 1 → 2 → 3
```

无论删除：

```
头节点
中间节点
尾节点
```

都统一处理。

最后：

```
返回：
dummy.next
```

即可。

这是链表删除题最经典技巧。

---

# 六、图解整个过程

例如：

```
1 → 2 → 3 → 4 → 5

n = 2
```

建立：

```
dummy
↓
0 → 1 → 2 → 3 → 4 → 5
```

初始化：

```
slow
↓
dummy

fast
↓
dummy
```

---

## 第一步

让：

```
fast
先走
2 步
```

结果：

```
dummy → 1 → 2
            ↑
          fast
```

slow：

仍然：

```
dummy
```

---

## 第二步

一起移动。

第一次：

```
slow
↓
1

fast
↓
3
```

第二次：

```
slow
↓
2

fast
↓
4
```

第三次：

```
slow
↓
3

fast
↓
5
```

第四次：

```
slow
↓
3

fast
↓
None
```

停止。

现在：

```
slow
正好在：
待删除节点
4
前面。
```

---

## 第三步

修改：

```python
slow.next = slow.next.next
```

原来：

```
3 → 4 → 5
```

修改后：

```
3 → 5
```

完成删除。

---

# 七、为什么 fast 要先走 n+1 步？

因为：

我们希望：

```
fast
和
slow
之间始终保持：
n+1
个节点距离。
```

这样：

当：

```
fast = None
```

时：

```
slow
就在：
删除节点
前一个位置。
```

例如：

```
dummy → 1 → 2 → 3 → 4 → 5
```

删除：

```
4
```

最终：

```
slow
↓
3
```

刚好方便修改：

```
slow.next
```

因此：

**使用 Dummy 时，Fast 需要先走 `n + 1` 步。**

---

# 八、LeetCode 核心代码模式（★★★★★）

```python
class Solution:
    def removeNthFromEnd(
        self,
        head: ListNode,
        n: int
    ) -> ListNode:

        # 创建虚拟头节点
        dummy = ListNode(0)
        dummy.next = head

        # 快慢指针
        fast = dummy
        slow = dummy

        # fast 先走 n + 1 步
        for _ in range(n + 1):
            fast = fast.next

        # 同时移动
        while fast:

            fast = fast.next
            slow = slow.next

        # 删除节点
        slow.next = slow.next.next

        return dummy.next
```

---

# 九、逐句代码讲解

---

## ① 创建 Dummy

```python
dummy = ListNode(0)
dummy.next = head
```

作用：建立：

```
虚拟头节点。
```

例如：

```
dummy
↓
0 → 1 → 2 → 3
```

这样：删除：

```
头节点
```

也不用特殊处理。

---

## ② 初始化快慢指针

```python
fast = dummy
slow = dummy
```

表示：

```
两个指针
都从 Dummy 开始。
```

---

## ③ fast 先走 n+1 步

```python
for _ in range(n + 1):
    fast = fast.next
```

例如：

```
n = 2
```

则：

```
fast
先走：
3 步。
```

目的：

保持：

```
fast
与
slow

间隔：
n+1
```

---

## ④ 同时移动

```python
while fast:
```

表示：

```
只要：fast
还没结束。
```

继续：

```python
fast = fast.next
slow = slow.next
```

保持：

```
距离 一直不变。
```

---

## ⑤ 删除节点

```python
slow.next = slow.next.next
```

这是本题最核心一句。

例如：

原来：

```
3 → 4 → 5
```

修改：

```
3 → 5
```

节点：

```
4
```

自然：失去引用。
被删除。

---

## ⑥ 返回结果

```python
return dummy.next
```

真正头节点：

不是：

```
dummy
```

而是：

```
dummy.next
```

---

# 十、为什么不能写成 slow = slow.next.next？

错误：

```python
slow = slow.next.next
```

只是：

```
改变了：slow变量。
```

并没有：修改链表。

例如：
```
1 → 2 → 3
```

仍然：存在。

---

必须：修改：

```
next
```

正确：

```python
slow.next = slow.next.next
```

---

# 十一、LeetCode 极简模板（★★★★★）

```python
dummy = ListNode(0)
dummy.next = head

fast = slow = dummy

for _ in range(n + 1):
    fast = fast.next

while fast:

    fast = fast.next
    slow = slow.next

slow.next = slow.next.next

return dummy.next
```

记忆口诀：

```
Dummy 开头最省心，

Fast 先跑 n+1，

一起移动到结尾，

Slow 前驱改 next。
```

---

# 十二、ACM 模式写法（面试常用）

```python
def removeNthFromEnd(head, n):

    dummy = ListNode(0)
    dummy.next = head

    fast = dummy
    slow = dummy

    for _ in range(n + 1):
        fast = fast.next

    while fast:

        fast = fast.next
        slow = slow.next

    slow.next = slow.next.next

    return dummy.next
```

---

# 十三、涉及的重要 Function / Python 语法讲解

---

## 1. `ListNode()`

作用：
创建一个新的链表节点。

例如：

```python
node = ListNode(5)
```

得到：

```
5 → None
```

本题：

```python
dummy = ListNode(0)
```

创建：

```
虚拟头节点。
```

---

## 2. `.next`

作用：

```python
node.next
```

表示：

```
当前节点的下一个节点。
```

例如：

```
2 → 3 → 4
```

对于：

```
node = 3
```

有：

```python
node.next
```

得到：

```
4
```

---

## 3. `range()`

格式：

```python
range(k)
```

作用：循环：

```
k 次。
```

例如：

```python
for _ in range(3):
```

执行：

```
3 次。
```

这里：

```python
range(n + 1)
```

表示：

```
Fast先走：n+1步。
```

---

## 4. `for _ in range()`

为什么：

```
_
```

因为：

```
循环变量 不用。
```

Python 中：

```
_
表示：占位变量。
```

例如：

```python
for _ in range(5):
```

表示：

```
重复：5 次。
```

---

# 十四、复杂度分析

时间复杂度：

```
O(n)
```

整个链表：只遍历一次。

---

空间复杂度：

```
O(1)
```

只使用：

```
dummy
fast
slow
```

三个额外指针。

---

# 十五、链表 Dummy + 快慢指针模板总结（★★★★★）

这是链表删除类问题最经典模板。

```python
dummy = ListNode(0)
dummy.next = head

fast = slow = dummy

for _ in range(n + 1):
    fast = fast.next

while fast:

    fast = fast.next
    slow = slow.next

slow.next = slow.next.next

return dummy.next
```

适用于：

- 19. 删除链表的倒数第 N 个节点
- 61. 旋转链表（思想类似）
- 链表间距类问题

---

# 十六、与其他链表题的区别

| 题目 | 核心思想 | 是否使用 Dummy | 是否使用快慢指针 |
|------|----------|----------------|------------------|
| 2. 两数相加 | Dummy + Carry | ✔ | ✘ |
| 21. 合并有序链表 | Dummy + 双指针 | ✔ | ✘ |
| 19. 删除倒数第 N 个节点 | Dummy + 快慢指针 | ✔ | ✔ |
| 141. 环形链表 | 快慢指针判环 | ✘ | ✔ |
| 142. 环形链表 II | 快慢指针找入口 | ✘ | ✔ |

可以发现：

**19 是 Dummy 与快慢指针结合的经典代表题。**

---

# 十七、Hot100 链表专题知识串联

链表中的经典模板如下：

| 模板 | 代表题目 |
|------|----------|
| Dummy（虚拟头节点） | 2、19、21、23、82、86、203 |
| Dummy + 快慢指针 | 19 |
| 快慢指针 | 141、142、234、876 |
| 双指针遍历 | 160 |
| 链表反转 | 206、92、25 |

可以发现：

> **LeetCode 19 是链表"固定间距双指针"模板的起点，也是面试中删除节点类问题的高频题。**

---

# 十八、面试记忆口诀（★★★★★）

```text
Dummy 开头最保险，

Fast 先跑 n 加一。

两人同步往前走，

Slow 停在前驱里。

修改 next 删除点，

返回 next 才完毕。
```

---

# 最终总结

本题的本质：

```
利用 Fast 与 Slow 保持固定间距。

Fast 先走 n+1 步，

随后两者同步移动，

当 Fast 到达链表末尾时，

Slow 正好停在待删除节点的前一个位置，

修改 next 即可完成删除。
```

必须熟练掌握的核心代码：

```python
dummy = ListNode(0)
dummy.next = head

fast = slow = dummy

for _ in range(n + 1):
    fast = fast.next

while fast:
    fast = fast.next
    slow = slow.next

slow.next = slow.next.next

return dummy.next
```

这是 **LeetCode Hot100 链表专题中 Dummy + 快慢指针模板最经典的代表题**，也是面试中出现频率极高的链表删除问题，必须熟练掌握。